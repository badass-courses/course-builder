import { PostMetadataFormFields } from './edit-post-form-metadata'

/**
 * Metadata form fields for article posts.
 * Currently reuses the existing PostMetadataFormFields.
 */
export const ArticleMetadataFormFields = PostMetadataFormFields
