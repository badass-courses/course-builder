/**
 * A module for modeling user actions around commands.
 *
 * Primarily providing a command registry, this module also
 * provides a registry and utilities for implementing keybindings
 * and menus.
 *
 * @module
 */
