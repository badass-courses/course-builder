import * as React from 'react'

// This is a placeholder - we'll implement the actual split-view layout later
export default function EditLessonLayout({
	children,
}: {
	children: React.ReactNode
}) {
	return children
}
