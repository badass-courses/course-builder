import { describe, it, expect } from 'vitest'
import { {{camelCase functionName}} } from './{{kebabCase utilityName}}'

describe('{{camelCase functionName}}', () => {
  it('should handle basic functionality', () => {
    const result = {{camelCase functionName}}('test')
    expect(result).toBe('test')
  })

  it('should handle edge cases', () => {
    expect({{camelCase functionName}}('')).toBe('')
  })
})